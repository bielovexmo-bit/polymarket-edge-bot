version: "3.9"
services:
  polymarket-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./bot.log:/app/bot.log
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
